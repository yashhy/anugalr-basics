import { MyCitPipePipe } from './my-cit-pipe.pipe';

describe('MyCitPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MyCitPipePipe();
    expect(pipe).toBeTruthy();
  });
});
